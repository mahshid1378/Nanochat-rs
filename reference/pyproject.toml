[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = ["nanochat", "torch", "safetensors"]

[tool.uv.sources]
nanochat = { path = "./nanochat" }
